<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coupang.marketplace.repository.UserRepository">
    <insert id="insertUser" parameterType="User">
        INSERT INTO USER (register_info_index, password, name, age, sex, phoneNo, salt, createdAt, updatedAt)
        VALUES (#{register_info_index}, #{password}, #{name}, #{age}, #{sex}, #{phoneNo}, #{salt}, #{createdAt}, #{updatedAt})
    </insert>
    <select id="findByRegisterInfoIndex" resultType="User">
        SELECT register_info_index, password, name, age, sex, phoneNo, salt, createdAt, updatedAt
        FROM USER
        WHERE register_info_index = #{register_info_index}
    </select>
</mapper>